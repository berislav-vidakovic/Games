{
  "private": true,
  "workspaces": [
    "common",
    "panel",
    "sudoku",
    "connect4"    
  ],
  "scripts": {
    "rebuild": "npm run build -w panel && npm run build -w sudoku && npm run build -w connect4"
  },
  "dependencies": {
    "http-status-codes": "^2.3.0"
  }
}
